apply plugin: 'com.android.library'

group = 'ern.adapter.synchronizer'
version = "1.0.0"

android {
    compileSdkVersion 22
    buildToolsVersion "22.0.1"
    defaultConfig {
        minSdkVersion 15
        targetSdkVersion 22
        versionCode 1
        versionName version
    }
    buildTypes {
    }
}

def siteUrl = 'https://github.com/erencelik/adapter-synchronizer'
def gitUrl = 'https://github.com/erencelik/adapter-synchronizer.git'

dependencies {
}