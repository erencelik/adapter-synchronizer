buildscript {
    repositories {
        mavenCentral()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:1.3.0'
    }
}

allprojects {
    version = '1.0.0'
    group = 'ern.adapter.synchronizer'
    repositories {
        mavenCentral()
    }
}